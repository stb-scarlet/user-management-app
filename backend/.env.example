# --- Database ---
DB_HOST=your-mysql-host
DB_PORT=3306
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name
# Set to "true" if your provider requires SSL (e.g. Aiven)
DB_SSL=false
# Keep low on serverless platforms (Vercel) to avoid exhausting your
# MySQL provider's connection limit. 2 is a safe default.
DB_CONNECTION_LIMIT=2

# --- Auth ---
JWT_SECRET=replace-with-a-long-random-string

# --- Frontend URL (for CORS + confirmation links) ---
CLIENT_URL=http://localhost:5173

# --- E-mail (Gmail app password) ---
# Generate at https://myaccount.google.com/apppasswords
GMAIL_USER=youraccount@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password

# --- Server ---
PORT=5000
